# Sign-Language-Recognition
I built a real-time ML model that recognizes hand signs from a webcam and predicts alphabet letters.  The project uses MediaPipe Hands to extract hand landmarks and a Random Forest Classifier to classify the gestures.  I normalized the hand coordinates to improve accuracy regardless of hand position or distance from the camera.
